DROP TABLE IF EXISTS MONEY_BOOKING;

CREATE TABLE MONEY_BOOKING(
  id INT AUTO_INCREMENT  PRIMARY KEY,
  first_name VARCHAR(250) NOT NULL,
  last_name VARCHAR(250) NOT NULL,
  total_money INT DEFAULT 0
);

INSERT INTO MONEY_BOOKING (first_name, last_name, total_money) VALUES
('Aliko', 'Dangote', 100),
('Bill', 'Gates', 200),
('Folrunsho', 'HANAGA' 500);